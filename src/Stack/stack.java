package Stack;
import linked.SinglyLinkedList.*;

public class stack {
    @SuppressWarnings("unused")
    private LList<Integer> LinkedList;

    @SuppressWarnings({ "rawtypes", "unchecked" })
    public stack() {
        LinkedList = new LList();
    }

    //push

    //pop
}
